{
  "name": "visual-markdown-editor",
  "displayName": "Visual Markdown Editor",
  "description": "Edit Markdown visually inside VS Code.",
  "version": "0.0.1",
  "publisher": "yunsly",
  "private": true,
  "engines": {
    "vscode": "^1.125.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "contributes": {
    "customEditors": [
      {
        "viewType": "visualMarkdown.editor",
        "displayName": "Visual Markdown Editor",
        "selector": [
          {
            "filenamePattern": "*.md"
          }
        ],
        "priority": "option"
      }
    ]
  },
  "main": "./dist/extension.js",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\"",
    "build:extension": "node esbuild.mjs",
    "build": "npm run typecheck && npm run build:extension"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "24.13.3",
    "@types/vscode": "1.125.0",
    "eslint": "10.7.0",
    "esbuild": "0.28.1",
    "typescript-eslint": "8.64.0",
    "typescript": "6.0.3"
  }
}
